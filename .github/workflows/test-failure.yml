name: Test Failure Action

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches: [ main ]

jobs:
  intentional-failure:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: This step will fail
        run: |
          echo "Starting intentional failure test..."
          echo "This command will definitely fail:"
          nonexistent-command-that-will-fail
          
      - name: This won't run due to previous failure
        run: echo "This step should not execute. Why did we implement this? Just ridiculous."